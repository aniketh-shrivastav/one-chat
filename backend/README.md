# Backend

Express + MongoDB (Mongoose) server providing authentication and real-time chat (Socket.IO) APIs.

## Scripts

```
npm install
npm run start   # starts server at :5000
```

## Chat Feature

See `CHAT_FEATURE.md` for detailed documentation of models, endpoints, socket events, and enhancement roadmap.
